# results on generators and generating collections

- id: generator_consequence
  assumptions:
    - generator
  conclusions:
    - generating collection
    - inhabited
  proof: This is trivial.

- id: extremal_generator_consequence
  assumptions:
    - extremal generator
  conclusions:
    - extremal generating collection
    - generator
  proof: This is trivial.

- id: extremal_generating_collection_consequence
  assumptions:
    - extremal generating collection
  conclusions:
    - generating collection
  proof: This is trivial.

- id: generator_balanced_consequence
  assumptions:
    - generator
    - balanced
  conclusions:
    - extremal generator
  proof: This is immediate from the fact that any faithful functor out of a balanced category is also conservative (see <a href="/functor-implication/faithful_with_balanced_domain">here</a>).

- id: generating_collection_balanced_consequence
  assumptions:
    - generating collection
    - balanced
  conclusions:
    - extremal generating collection
  proof: This is immediate from the fact that any faithful functor out of a balanced category is also conservative (see <a href="/functor-implication/faithful_with_balanced_domain">here</a>).

- id: generator_via_coproduct
  assumptions:
    - coproducts
    - generating collection
    - strongly connected
  conclusions:
    - generator
  proof: We get this as a corollary of <a href="/content/generator_construction">this result.</a>

- id: extremal_generator_via_coproduct
  assumptions:
    - coproducts
    - extremal generating collection
    - strongly connected
  conclusions:
    - extremal generator
  proof: We get this as a corollary of <a href="/content/generator_construction">this result.</a>

- id: free-algebra-generates
  assumptions:
    - one-sorted finitary algebraic
  conclusions:
    - extremal generator
  proof: Pick an algebraic theory that represents the category. The free algebra $F(1)$ on one generator is an extremal generator since it represents the underlying set functor, which is faithful and conservative.

- id: locally-finite_left-cancellative_semi-strongly-connected_extremal-generating-set
  assumptions:
    - locally finite
    - left cancellative
    - semi-strongly connected
    - extremal generating collection
  conclusions:
    - essentially small
  proof: >-
    Suppose a category $\C$ is locally finite, left cancellative, semi-strongly connected, and has an extremal generating collection $S$. We then claim that
    $$\Ob(\C) \to \IN^S, \, X \mapsto \bigl(G \mapsto \card(\Hom(G, X))\bigr)$$
    is injective on isomorphism classes of $\Ob(\C)$. To see this, suppose two objects $X$ and $Y$ map to the same cardinality tuple. Since $\C$ is semi-strongly connected, we may assume without loss of generality that there is a morphism $f : X \to Y$. Then since $f$ is a monomorphism, for each $G \in S$ we have
    $$f \circ {-} : \Hom(G, X) \to \Hom(G, Y)$$
    is an injective function between finite sets of equal cardinality, and therefore is also a bijection. By the assumption that $S$ is an extremal generating collection, we thus have $f$ is an isomorphism.

    This shows that the collection of isomorphism classes of objects of $X$ is essentially small, i.e. isomorphic to a set. Together with the assumption that the category is locally finite, this implies the category is essentially small.
